version: "3.9"
services:
  ai-service:
    build: ./ai-service
    env_file:
      - ./ai-service/.env
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    env_file:
      - ./frontend/.env
    ports:
      - "5173:5173"
    depends_on:
      - ai-service
